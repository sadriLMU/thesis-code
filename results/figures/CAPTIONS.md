# Suggested captions for results/figures/*.png

Auto-generated by plot_circuits.py from the actual numbers of this run. Qiskit's circuit drawer does not render fidelity/gate-count text into the saved PNGs, so these numbers must come from the LaTeX caption, not the image. Copy/adapt into the relevant \caption{} calls; \ref{} targets assume label names matching the filenames (e.g. fig:best-circuit-ea).

### `best_circuit_ea.png`

Best circuit found by EA for target seed 49 (fidelity = 0.505, 5 gates -- close to the main comparison's mean of 4.96 gates, Table \ref{tab:main-comparison}, chosen as a representative rather than a best- or worst-case example).

### `best_circuit_sa.png`

Best circuit found by SA for the same target seed 49 (fidelity = 0.454, 5 gates -- close to the main comparison's mean of 6.26 gates). Shown for the same target as Figure~\ref{fig:best-circuit-ea} to allow a direct visual comparison of circuit length and gate composition between the two algorithms.

### `sa_neighbor_before.png / sa_neighbor_after.png`

One neighbor() step (Section~\ref{sec:sa-design}), shown before and after. Compare the two diagrams gate-by-gate to see which single operation (replace/insert/delete) was applied.

### `ea_crossover_parent1.png / ea_crossover_parent2.png / ea_crossover_child.png`

Two parent circuits and the resulting child, drawn per-qubit for reference. \textbf{Caution:} Qiskit's circuit drawer groups gates by qubit wire, not by their position in the flat gate list crossover() actually operates on, so the split point is not visible in these three diagrams alone -- use Figure~\ref{fig:crossover-trace} (\texttt{ea\_crossover\_trace.png}) to explain the mechanism, and cite these three only as "what a resulting circuit looks like."

### `ea_crossover_trace.png`

Single-point crossover (Section~\ref{sec:ea-design}), shown as the flat gate list rather than per-qubit, so the split point (index 1) and each gate's parental origin are directly visible. This is the recommended figure for explaining the crossover mechanism; Figure~\ref{fig:crossover-parents} shows the same example as an actual per-qubit circuit for reference.
